node_modules/
dist/
.env
.env.local
.env.*.local
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Exact Figma recovery archive; this is a backup, not active project source.
Follow Prompt Instructions.zip

# Unused files left by the abandoned refactor; do not include in the visual baseline.
.env.example
eslint.config.js
package-lock.json
README.md
vitest.config.ts
/src/components/media/
/src/config/
/src/features/
/src/lib/
/src/pages/Home/
