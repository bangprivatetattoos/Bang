[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Vite 8 requires Node 20.19+/22.12+; matches .mise.toml.
[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
